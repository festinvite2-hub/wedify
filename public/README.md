# Public assets

This folder is committed and used by Next.js for static files.

- `wedify-logo.png` is loaded in the app via `LOGO_SM = "/wedify-logo.png"`.

If this folder doesn't appear on GitHub, make sure the latest branch commit is pushed.
